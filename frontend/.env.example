# Frontend Environment Configuration
# Used by Vercel Project 1 (Frontend Instance)

# URL pointing to the deployed backend Vercel instance (or localhost:8000/api in development)
# In production, set this in Vercel to: https://<your-backend-project>.vercel.app/api
NEXT_PUBLIC_API_URL=http://localhost:8000/api
